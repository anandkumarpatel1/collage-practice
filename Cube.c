#include <stdio.h>
int main()
{
    int n, c, sum = 0;
    scanf("%d", &n);
    for (int i = 0; n != 0; i++)
    {
        c = n % 10;
        n = n / 10;
        for (int j = 0; j < 3; j++)
        {
            sum = sum + 
        }
        
    }
    printf("%d", sum);

    return 0;
}